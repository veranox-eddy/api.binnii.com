# api.binnii.com — internal signup-intake exposure.
#
# STAGING (same host): a loopback-only listener; the public vhost denies
# /internal/ outright.
#
#   server {                          # add to the existing api server block
#       location /api/internal/ { deny all; }
#   }
#   server {
#       listen 127.0.0.1:8082;
#       root /var/www/api.binnii.com/public;
#       index index.php;
#       location / { try_files $uri $uri/ /index.php?$query_string; }
#       location ~ \.php$ {
#           include snippets/fastcgi-php.conf;
#           fastcgi_pass unix:/run/php/php8.3-fpm.sock;
#       }
#   }
#
# PRODUCTION (split hosts): source-IP allowlist + mTLS on the public vhost,
# HMAC stays as the second layer.
#
#   ssl_verify_client on;                        # signup host client cert
#   ssl_client_certificate /etc/nginx/ssl/binnii-internal-ca.pem;
#   location /api/internal/ {
#       allow <signup-host-ip>;
#       deny all;
#       try_files $uri $uri/ /index.php?$query_string;
#   }
